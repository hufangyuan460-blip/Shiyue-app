# AGENTS.md

## 项目说明

本项目是“拾页”Android 实体书阅读记录 App，用于记录实体书阅读进度、阅读时长、阅读场次和随记。

## 开发前必读

开始任何开发工作前，必须完整阅读并遵循：

- `README.md`
- `docs/product-design.md`
- `docs/development.md`

如实现与文档冲突，应先明确并同步产品或开发文档，不能静默改变既定业务规则。

## 技术与架构规范

- 使用 Kotlin、Jetpack Compose、Material 3、Room、Hilt、Coroutines 和 Flow。
- 核心功能必须离线可用；网络仅用于 ISBN 书籍资料查询和封面下载，查询失败或断网时必须支持本地降级流程。
- UI 必须通过 ViewModel、Use Case 和 Repository 访问数据，不得直接访问 DAO 或网络接口。
- 阅读计时不能仅依赖内存倒计时，必须支持进入后台、锁屏以及进程重建后的准确恢复。
- 分层依赖应保持清晰，避免将业务规则放入 Compose UI、DAO 或网络实现中。

## 数据与测试规范

- 修改核心业务规则时，必须添加或更新相应的自动化测试。
- 修改 Room 数据结构时，必须提供 Migration、更新 Schema，并添加迁移测试；不得使用破坏性迁移代替正式迁移。
- 涉及计时、页码进度、回读、统计、备份恢复等逻辑时，应覆盖边界条件和异常恢复场景。

## 安全与 Git 规范

- 不得硬编码或提交 API Key、密码、访问令牌、签名文件、私钥、真实凭据、本机绝对路径等敏感信息。
- 本机配置应放入已忽略的文件或通过安全的环境配置注入。
- 未经用户明确要求，不得自动连接、创建或推送 GitHub 远端，不得自动执行 `git push`。
- 不得擅自覆盖或删除现有产品与开发文档。

## 任务交付

完成任务后必须汇报：

- 创建或修改的文件；
- 执行的验证及结果；
- 未解决问题、假设和遗留风险。
